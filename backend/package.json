{
  "name": "rakett-chat-backend",
  "version": "1.0.0",
  "description": "Backend for Rakett Chat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node src/index.ts",
    "start": "node -r tsconfig-paths/register dist/index.js",
    "watch": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "dotenv": "^17.2.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@azure/identity": "^4.12.0",
    "@azure/web-pubsub": "^1.2.0",
    "express": "^5.1.0"
  }
}